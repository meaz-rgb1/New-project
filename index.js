
       document.querySelector('#alert').
       addEventListener ('click', function () => (alert
       ('I like Red Panda')

       function alertMe (){
           return alert  ('I like Red Panda')
       }
           e.preventDefault()
           let animalObj= {
               name:e.target.name.value,
               imageUrl:e.target.image_Url.value,
               description.target.description.value,
           }
                   
 function getRed-Panda (animalObj){
     fetch('"https://i.imgur.com/VI6Vh88.png",')
     .then(resv=> resv.json())
     .then(animalData => animalDatea.forEach(animal=>renderOneAnimal(animal)))
         console.log (Red panda.db)
 }
      
